tileServer:
  host: api.gbif.org
  port: 80
  prefix: /v2
#  host: "localhost"
#  port: 7001
#  prefix: ""
  timeout: 10000

# If set, service will register with ZooKeeper
#service:
# zkHost: localhost:2181
# zkPath: local/services
# host: localhost

logging:
  timing: true
