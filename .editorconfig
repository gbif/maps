# GBIF .EDITORCONFIG

# Unix-style newlines with a newline ending every file
[*]
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# 2 space indentation
[*.{html,java,js,sql,xml,yaml,yml}]
indent_style = space
indent_size = 2
